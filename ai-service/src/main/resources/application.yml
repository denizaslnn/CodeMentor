server:
  port: 8083

spring:
  application:
    name: ai-service
  data:
    redis:
      host: localhost
      port: 6379
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

rabbitmq:
  queue:
    name: code.analysis.queue
